<%= form_for(@store) do |f| %>
  <% if @store.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@store.errors.count, "error") %> prohibited this store from being saved:</h2>

      <ul>
      <% @store.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="field">
    <%= f.label 'X Value' %><br />
	<%= number_field_tag( 'xval', 1, :in => 1..20) %>
  </div>
  
  <div class="field">
    <%= f.label 'Y Value' %><br />
	<%= number_field_tag( 'yval', 1, :in => 1..20) %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
